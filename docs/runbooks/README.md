# Runbooks

Operational runbooks live here. They should be written so a deployer can operate their own instance without access to any maintainer-owned account.

Runbooks must reference secret locations and rotation steps, not raw secret values.
